import { Box, styled } from '@mui/material'

const BoxLineBlue = styled(Box)(({ theme }) => ({
  backgroundColor: '#1c2235',
  width: '10%',
  height: '.3em',
  marginBottom: '2px',
}))

export default BoxLineBlue
